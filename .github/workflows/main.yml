name: CI

on:
  push:
    branches: v7.3
  pull_request:

jobs:
  pipeline:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Prepare
        run: |
          docker build --file development/Dockerfile \
                       --tag ghcr.io/gaming-platform/docker-php-fpm:7.3-development \
                       .
          docker build --file production/Dockerfile \
                       --tag ghcr.io/gaming-platform/docker-php-fpm:7.3-production \
                       .

      - name: Deploy
        if: github.ref == 'refs/heads/v7.3'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
          docker push ghcr.io/gaming-platform/docker-php-fpm:7.3-development
          docker push ghcr.io/gaming-platform/docker-php-fpm:7.3-production
